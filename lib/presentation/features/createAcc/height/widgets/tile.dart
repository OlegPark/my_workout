import 'package:flutter/material.dart';

class MtTile extends StatelessWidget {
  const MtTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
      child: Center(child: Text('Hello')),
    );
  }
}